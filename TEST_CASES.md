# TEST_CASES

## 1. 火警报警
- 测试目标：验证 `raw_event -> alarm -> notification -> realtime -> audit_log` 全链路。
- 前置条件：存在企业设备；通知模板已启用；企业端首页、`/alarm-center`、`/devices` 已登录。
- 操作步骤：
  1. 调用 `POST /api/ingestion/event`，`event_type=alarm`。
  2. 查看 `/alarm-center`。
  3. 查看 `/notification-center`。
  4. 查看 `/audit-log`。
- 预期结果：
  - `raw_device_events` 写入 1 条。
  - `device_status_snapshots` 和设备状态变为报警。
  - 报警中心新增 1 条正式报警。
  - 通知中心生成通知记录。
  - 首页、报警中心、设备页收到实时更新。
  - 审计日志有 `device.event.ingested`、`alarm.created`、`notification.triggered`。

## 2. 设备故障
- 测试目标：验证故障事件按故障类报警处理。
- 前置条件：存在可用设备。
- 操作步骤：
  1. 发送 `event_type=fault`。
  2. 查看报警中心和设备页。
- 预期结果：
  - 设备状态变为故障。
  - 报警中心新增故障类报警。
  - 通知等级为 `fault`。

## 3. 设备离线
- 测试目标：验证离线事件只更新设备状态，不误建正式报警。
- 前置条件：存在可用设备。
- 操作步骤：
  1. 发送 `event_type=offline`。
  2. 查看报警中心和设备页。
- 预期结果：
  - 设备状态变为离线。
  - 报警中心不新增正式报警。
  - 审计日志记录状态更新。

## 4. 设备恢复
- 测试目标：验证恢复事件不误关正式报警。
- 前置条件：该设备已有未闭环正式报警。
- 操作步骤：
  1. 发送 `event_type=recovery`。
  2. 查看设备状态和报警中心。
- 预期结果：
  - 设备状态恢复正常。
  - 已存在的正式报警仍保持未闭环。
  - 报警时间轴追加“恢复事件”日志。

## 5. 心跳事件
- 测试目标：验证心跳更新在线状态但不生成正式报警。
- 前置条件：存在在线设备。
- 操作步骤：
  1. 发送 `event_type=heartbeat`。
  2. 查看设备状态和报警中心。
- 预期结果：
  - `last_report_at` 更新。
  - 无新正式报警。
  - 若设备存在未闭环报警，设备异常状态不被误覆盖。

## 6. 同一设备重复报警
- 测试目标：验证 ingestion 幂等去重。
- 前置条件：准备同一 `event_id` 或同一 dedupe key 的报警事件。
- 操作步骤：
  1. 连续两次发送相同事件。
  2. 查询报警中心和 `raw_device_events`。
- 预期结果：
  - 正式报警只创建 1 条。
  - 第二次返回 `duplicate_suppressed=true`。
  - 报警时间轴记录重复事件。

## 7. 跨日未闭环报警
- 测试目标：验证首页保留跨日未闭环。
- 前置条件：数据库中存在昨天创建、今天仍未闭环的报警。
- 操作步骤：
  1. 进入首页。
  2. 查看实时警情和统计卡。
- 预期结果：
  - 实时警情显示跨日未闭环。
  - 今日报警数不重复计入昨天事件。
  - 未闭环统计包含跨日未闭环。

## 8. 值班交接班时存在未处理报警
- 测试目标：验证交接班强制备注。
- 前置条件：当前租户存在未闭环报警。
- 操作步骤：
  1. 在 `/duty-center` 发起交接班，不填写备注。
  2. 再填写备注提交。
- 预期结果：
  - 第一次返回 400。
  - 第二次成功。
  - 值班日志记录交接班。

## 9. 巡检异常生成隐患
- 测试目标：验证巡检异常自动转隐患。
- 前置条件：存在巡检任务。
- 操作步骤：
  1. 在 `/inspection` 对任务执行“标记异常”。
  2. 查看隐患列表。
- 预期结果：
  - 新增 1 条隐患。
  - 任务状态更新。
  - 审计日志记录巡检异常。

## 10. 隐患整改闭环
- 测试目标：验证隐患状态流转。
- 前置条件：已有隐患记录。
- 操作步骤：
  1. 依次将隐患改为“整改中”“已整改”“已复查”。
- 预期结果：
  - 状态逐步更新。
  - 时间线/审计日志保留记录。

## 11. 套餐限制导致功能不可用
- 测试目标：验证功能受套餐约束。
- 前置条件：企业套餐未开通某模块。
- 操作步骤：
  1. 登录未开通该模块的企业账号。
  2. 访问对应页面。
- 预期结果：
  - 页面显示不可用或限制提示。
  - 不允许越权调用 backend 数据接口。

## 12. 多租户数据隔离验证
- 测试目标：验证 tenant 间数据绝对隔离。
- 前置条件：tenant A 和 tenant B 均有设备、报警。
- 操作步骤：
  1. 用 tenant A 访问 `/alarm-center`、`/devices`。
  2. 对 tenant A 设备发送事件。
  3. 用 tenant B 查看同类页面。
- 预期结果：
  - tenant B 不看到 tenant A 的设备和报警。
  - backend 查询严格按 tenant_id 过滤。

## 13. 未授权用户访问其他企业数据
- 测试目标：验证跨租户访问被拒绝。
- 前置条件：构造错误 `x-tenant-id` 或错误会话。
- 操作步骤：
  1. 用 tenant A 会话访问 tenant B 的接口。
- 预期结果：
  - 返回 403 或空数据。
  - 审计日志记录异常访问。

## 14. 通知失败重试
- 测试目标：验证通知失败后可重试。
- 前置条件：存在 `failed` 状态通知记录。
- 操作步骤：
  1. 调用 `/api/tenant/notification-center` PATCH 重试。
  2. 查看通知中心。
- 预期结果：
  - `retry_count + 1`。
  - 状态变为 `sent`。
  - 审计/错误统计相应更新。

## 15. 审计日志落库校验
- 测试目标：验证关键操作都有审计记录。
- 前置条件：执行登录、报警处理、设备操作、巡检/交接班。
- 操作步骤：
  1. 查询 `/audit-log`。
- 预期结果：
  - 至少能看到登录、设备事件接入、报警创建/处理、值班交接、巡检异常记录。
